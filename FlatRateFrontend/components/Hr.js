import { View } from "react-native"
import colors from "../colors"

export default () => {
    return (
        <View style={{borderBottomWidth:1, borderBottomColor: colors.purple, alignSelf:"stretch", marginHorizontal:25}}></View>
    )
}